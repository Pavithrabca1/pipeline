pipeline {
  agent: any
  stages {
   stage('Build') {
     echo " Build Successfully"
   }
   stage('Test') {
     echo " Test Successfully"
   }
   stage('Deploy') {
     echo "Deploy Successfully"
   }
  }
}
